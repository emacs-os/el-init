# static-builds

Purpose:

1. provide the correct path to create a plain Emacs static binary, of a few levels of bloatedness.

2. provide additional pkgbuild/nix files for people who want to build the static emacs, patch it for pid1 support, and bake-in supervisor.el
